{
  "name": "hairdresser",
  "version": "0.1.4",
  "description": "A universal js library for managing head DOM elements (title, link, and meta)",
  "main": "lib/Hairdresser.js",
  "jsnext:main": "src/Hairdresser.js",
  "author": "Giff Song <giff.song@gmail.com> (http://github.com/pawsong)",
  "repository": {
    "type": "git",
    "url": "https://github.com/pawsong/hairdresser.git"
  },
  "license": "MIT",
  "keywords": [
    "head",
    "document",
    "meta",
    "link",
    "title",
    "angular",
    "react"
  ],
  "scripts": {
    "lint": "eslint ./",
    "test": "npm run lint && npm run test:all",
    "test:all": "karma start --single-run && npm run test:node",
    "test:cov": "COVERAGE=true karma start --single-run",
    "test:watch": "karma start",
    "test:node": "node test/jasmine-runner",
    "build:lib": "NODE_ENV=production babel src --optional utility.inlineEnvironmentVariables --out-dir lib",
    "build:umd": "webpack --config webpack/development.config.js",
    "build:umd:min": "webpack --config webpack/production.config.js",
    "build": "npm run build:lib && npm run build:umd && npm run build:umd:min",
    "docs:build": "jsdoc src -d .jsdoc -c ./jsdoc.json",
    "docs:publish": "npm run docs:build && gh-pages -d .jsdoc",
    "deploy": "npm test && npm run build && npm run deployment && npm run docs:publish",
    "deployment": "git add lib dist && npm version ${BUMP:-\"patch\"} --no-git-tag-version && git add package.json && git commit -am \"Autogenerated pre-deployment commit\" && bower version ${BUMP:-\"patch\"} && git reset HEAD~2 && git add . && git commit -am \"Release $(cat package.json | jq -r .version)\" && git push --tags && npm publish && git push"
  },
  "dependencies": {
    "invariant": "^2.1.1",
    "object-keys": "^1.0.7"
  },
  "devDependencies": {
    "babel": "^5.8.23",
    "babel-core": "^5.8.23",
    "babel-eslint": "^4.0.10",
    "babel-loader": "^5.3.2",
    "eslint": "^1.5.1",
    "eslint-config-airbnb": "0.0.9",
    "eslint-plugin-react": "^3.3.2",
    "fbemitter": "^2.0.0",
    "gh-pages": "^0.4.0",
    "isparta-loader": "^1.0.0",
    "jasmine": "^2.3.2",
    "jasmine-core": "^2.3.4",
    "jasmine-spec-reporter": "^2.4.0",
    "jsdoc": "git+https://github.com/jsdoc3/jsdoc.git",
    "karma": "^0.13.9",
    "karma-chrome-launcher": "^0.2.0",
    "karma-coverage": "^0.5.0",
    "karma-coveralls": "^1.1.2",
    "karma-jasmine": "^0.3.6",
    "karma-mocha-reporter": "^1.1.1",
    "karma-phantomjs-launcher": "^0.2.1",
    "karma-sourcemap-loader": "^0.3.5",
    "karma-webpack": "^1.7.0",
    "lodash": "^3.10.1",
    "phantomjs": "^1.9.18",
    "phantomjs-polyfill": "0.0.1",
    "rimraf": "^2.4.2",
    "webpack": "^1.12.0"
  }
}
